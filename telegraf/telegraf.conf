# Global tags can be specified here in key="value" format.
[global_tags]
  # dc = "us-east-1" # will tag all metrics with dc=us-east-1
  # rack = "1a"
  ## Environment variables can be used as tags, and throughout the config file
  # user = "$USER"


# Configuration for telegraf agent
[agent]
  ## Default data collection interval for all inputs
  interval = "10s"
  ## Rounds collection interval to 'interval'
  ## ie, if interval="10s" then always collect on :00, :10, :20, etc.
  round_interval = true

  ## Telegraf will send metrics to outputs in batches of at most
  ## metric_batch_size metrics.
  ## This controls the size of writes that Telegraf sends to output plugins.
  metric_batch_size = 1000

  ## For failed writes, telegraf will cache metric_buffer_limit metrics for each
  ## output, and will flush this buffer on a successful write. Oldest metrics
  ## are dropped first when this buffer fills.
  ## This buffer only fills when writes fail to output plugin(s).
  metric_buffer_limit = 10000

  ## Collection jitter is used to jitter the collection by a random amount.
  ## Each plugin will sleep for a random time within jitter before collecting.
  ## This can be used to avoid many plugins querying things like sysfs at the
  ## same time, which can have a measurable effect on the system.
  collection_jitter = "1s"

  ## Default flushing interval for all outputs. You shouldn't set this below
  ## interval. Maximum flush_interval will be flush_interval + flush_jitter
  flush_interval = "10s"
  ## Jitter the flush interval by a random amount. This is primarily to avoid
  ## large write spikes for users running a large number of telegraf instances.
  ## ie, a jitter of 5s and interval 10s means flushes will happen every 10-15s
  flush_jitter = "1s"

  ## By default or when set to "0s", precision will be set to the same
  ## timestamp order as the collection interval, with the maximum being 1s.
  ##   ie, when interval = "10s", precision will be "1s"
  ##       when interval = "250ms", precision will be "1ms"
  ## Precision will NOT be used for service inputs. It is up to each individual
  ## service input to set the timestamp at the appropriate precision.
  ## Valid time units are "ns", "us" (or "µs"), "ms", "s".
  precision = ""

  ## Logging configuration:
  ## Run telegraf with debug log messages.
  debug = false
  ## Run telegraf in quiet mode (error log messages only).
  quiet = false
  ## Specify the log file name. The empty string means to log to stderr.
  logfile = ""

  ## Override default hostname, if empty use os.Hostname()
  hostname = ""
  ## If set to true, do no set the "host" tag in the telegraf agent.
  ## Disabled here: the container hostname changes on every recreate, which would
  ## otherwise create duplicate per-unit series. The "unit" tag identifies devices.
  omit_hostname = true


###############################################################################
#                            OUTPUT PLUGINS                                   #
###############################################################################

# Configuration for sending metrics to InfluxDB 2.x
[[outputs.influxdb_v2]]
  ## The URLs of the InfluxDB cluster nodes.
  ##
  ## Multiple URLs can be specified for a single cluster, only ONE of the
  ## urls will be written to each interval.
  ##   ex: urls = ["https://us-west-2-1.aws.cloud2.influxdata.com"]
  urls = ["http://av_influxdb2:8086"]

  ## Local address to bind when connecting to the server
  ## If empty or not set, the local address is automatically chosen.
  # local_address = ""

  ## Token for authentication.
  token = "${INFLUXDB_ADMIN_TOKEN}"

  ## Organization is the name of the organization you wish to write to.
  organization = "docs"

  ## Destination bucket to write into.
  bucket = "home"

  ## The value of this tag will be used to determine the bucket.  If this
  ## tag is not set the 'bucket' option is used as the default.
  # bucket_tag = ""

  ## If true, the bucket tag will not be added to the metric.
  # exclude_bucket_tag = false

  ## Timeout for HTTP messages.
  timeout = "5s"

  ## Additional HTTP headers
  # http_headers = {"X-Special-Header" = "Special-Value"}

  ## HTTP Proxy override, if unset values the standard proxy environment
  ## variables are consulted to determine which proxy, if any, should be used.
  # http_proxy = "http://corporate.proxy:3128"

  ## HTTP User-Agent
  # user_agent = "telegraf"

  ## Content-Encoding for write request body, can be set to "gzip" to
  ## compress body or "identity" to apply no encoding.
  # content_encoding = "gzip"

  ## Enable or disable uint support for writing uints influxdb 2.0.
  # influx_uint_support = false

  ## When true, Telegraf will omit the timestamp on data to allow InfluxDB
  ## to set the timestamp of the data during ingestion. This is generally NOT
  ## what you want as it can lead to data points captured at different times
  ## getting omitted due to similar data.
  # influx_omit_timestamp = false

  ## HTTP/2 Timeouts
  ## The following values control the HTTP/2 client's timeouts. These settings
  ## are generally not required unless a user is seeing issues with client
  ## disconnects. If a user does see issues, then it is suggested to set these
  ## values to "15s" for ping timeout and "30s" for read idle timeout and
  ## retry.
  ##
  ## Note that the timer for read_idle_timeout begins at the end of the last
  ## successful write and not at the beginning of the next write.
  # ping_timeout = "0s"
  # read_idle_timeout = "0s"

  ## Optional TLS Config for use on HTTP connections.
  # tls_ca = "/etc/telegraf/ca.pem"
  # tls_cert = "/etc/telegraf/cert.pem"
  # tls_key = "/etc/telegraf/key.pem"
  ## Use TLS but skip chain & host verification
  # insecure_skip_verify = false

  ## Rate limits for sending data (disabled by default)
  ## Available, uncompressed payload size e.g. "5MB"
  # rate_limit = "unlimited"
  ## Fixed time-window for the available payload size e.g. "5m"
  # rate_limit_period = "0s"

  ## Number of concurrent writes to the output
  ## When set to one sequential sending is used (default).
  ## NOTE: When using two or more concurrent writes the sending order of
  ##       metrics is not guaranteed!
  # concurrent_writes = 1



###############################################################################
#                            INPUT PLUGINS                                    #
###############################################################################


# Kiloview N5/N6 NDI units.
#
# These devices return the auth token in the login response BODY (not a
# Set-Cookie header), so Telegraf's cookie_auth_url cannot capture it. Instead
# scripts/kiloview.sh logs in, sends the token back as the "token" cookie, and
# prints one JSON object per unit. The exec input parses that JSON.
#
# This approach is also resilient: an offline or bad-credential unit just emits
# {"up":0} and exits 0, so it never aborts the agent (unlike cookie_auth_url,
# whose login runs in Init() and is fatal on connection failure). All units can
# therefore stay enabled regardless of their current state.
#
# Per-unit "up" field: 1 = reachable and authenticated, 0 = down/unauthorized.

[[inputs.exec]]
  commands = ["/etc/telegraf/scripts/kiloview.sh ${NDI_RX001_HOST} RX001"]
  timeout = "10s"
  data_format = "json"
  name_override = "kiloview"
  json_string_fields = ["name", "codec", "resolution", "ip", "url", "warning", "video_mode", "audio_format", "persis", "start_time"]
  [inputs.exec.tags]
    unit = "RX001"

[[inputs.exec]]
  commands = ["/etc/telegraf/scripts/kiloview.sh ${NDI_RX002_HOST} RX002"]
  timeout = "10s"
  data_format = "json"
  name_override = "kiloview"
  json_string_fields = ["name", "codec", "resolution", "ip", "url", "warning", "video_mode", "audio_format", "persis", "start_time"]
  [inputs.exec.tags]
    unit = "RX002"

[[inputs.exec]]
  commands = ["/etc/telegraf/scripts/kiloview.sh ${NDI_RX003_HOST} RX003"]
  timeout = "10s"
  data_format = "json"
  name_override = "kiloview"
  json_string_fields = ["name", "codec", "resolution", "ip", "url", "warning", "video_mode", "audio_format", "persis", "start_time"]
  [inputs.exec.tags]
    unit = "RX003"

[[inputs.exec]]
  commands = ["/etc/telegraf/scripts/kiloview.sh ${NDI_RX004_HOST} RX004"]
  timeout = "10s"
  data_format = "json"
  name_override = "kiloview"
  json_string_fields = ["name", "codec", "resolution", "ip", "url", "warning", "video_mode", "audio_format", "persis", "start_time"]
  [inputs.exec.tags]
    unit = "RX004"

[[inputs.exec]]
  commands = ["/etc/telegraf/scripts/kiloview.sh ${NDI_RX005_HOST} RX005"]
  timeout = "10s"
  data_format = "json"
  name_override = "kiloview"
  json_string_fields = ["name", "codec", "resolution", "ip", "url", "warning", "video_mode", "audio_format", "persis", "start_time"]
  [inputs.exec.tags]
    unit = "RX005"

[[inputs.exec]]
  commands = ["/etc/telegraf/scripts/kiloview.sh ${NDI_RX006_HOST} RX006"]
  timeout = "10s"
  data_format = "json"
  name_override = "kiloview"
  json_string_fields = ["name", "codec", "resolution", "ip", "url", "warning", "video_mode", "audio_format", "persis", "start_time"]
  [inputs.exec.tags]
    unit = "RX006"

[[inputs.exec]]
  commands = ["/etc/telegraf/scripts/kiloview.sh ${NDI_RX007_HOST} RX007"]
  timeout = "10s"
  data_format = "json"
  name_override = "kiloview"
  json_string_fields = ["name", "codec", "resolution", "ip", "url", "warning", "video_mode", "audio_format", "persis", "start_time"]
  [inputs.exec.tags]
    unit = "RX007"

[[inputs.exec]]
  commands = ["/etc/telegraf/scripts/kiloview.sh ${NDI_RX008_HOST} RX008"]
  timeout = "10s"
  data_format = "json"
  name_override = "kiloview"
  json_string_fields = ["name", "codec", "resolution", "ip", "url", "warning", "video_mode", "audio_format", "persis", "start_time"]
  [inputs.exec.tags]
    unit = "RX008"

# RX009 currently rejects the shared NDI_API_USER/NDI_API_PASS (login error
# 0111003). It is safe to leave enabled — it just reports up=0 until the
# credentials are fixed in the device web UI.
[[inputs.exec]]
  commands = ["/etc/telegraf/scripts/kiloview.sh ${NDI_RX009_HOST} RX009"]
  timeout = "10s"
  data_format = "json"
  name_override = "kiloview"
  json_string_fields = ["name", "codec", "resolution", "ip", "url", "warning", "video_mode", "audio_format", "persis", "start_time"]
  [inputs.exec.tags]
    unit = "RX009"